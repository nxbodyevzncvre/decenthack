{
    "version": "0.2.0",
    "configurations": [
        


        {
            "name": "Launch Package",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/backend/cmd",
            "env": {
                "CONFIG_PATH": "${workspaceFolder}/backend/config/config.yaml"
            }
        }
    ]
}